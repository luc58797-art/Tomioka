<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
<meta name="theme-color" content="#050914">
<title>Aventura Tomioka</title>

<style>
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    -webkit-tap-highlight-color: transparent;
}

html,
body {
    width: 100%;
    height: 100%;
    overflow: hidden;
    background: #050914;
    touch-action: none;
    user-select: none;
    -webkit-user-select: none;
}

body {
    font-family: Arial, sans-serif;
}

canvas {
    display: block;
    width: 100vw;
    height: 100vh;
    touch-action: none;
}

#loading {
    position: fixed;
    inset: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 12px;
    background: radial-gradient(circle at center, #102b45, #050914 70%);
    color: white;
    font-size: 24px;
    font-weight: bold;
}

#loading small {
    font-size: 13px;
    opacity: .6;
    font-weight: normal;
}

#startScreen {
    position: fixed;
    inset: 0;
    z-index: 90;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    background: linear-gradient(180deg, rgba(4,15,31,.92), rgba(3,7,18,.98));
    color: white;
    text-align: center;
}

#startScreen h1 {
    font-size: clamp(30px, 8vw, 58px);
    margin-bottom: 8px;
    text-shadow: 0 0 25px rgba(56,189,248,.8);
}

#startScreen p {
    opacity: .7;
    margin-bottom: 25px;
}

#startButton {
    border: 2px solid rgba(125,211,252,.8);
    background: linear-gradient(135deg, #0369a1, #1d4ed8);
    color: white;
    padding: 16px 38px;
    border-radius: 18px;
    font-size: 18px;
    font-weight: bold;
    box-shadow: 0 8px 30px rgba(14,165,233,.3);
}

#startButton:active {
    transform: scale(.94);
}

.hidden {
    display: none !important;
}
</style>
</head>

<body>

<div id="loading">
    🌊 AVENTURA TOMIOKA
    <small>Carregando...</small>
</div>

<div id="startScreen" class="hidden">
    <h1>🌊 TOMIOKA</h1>
    <p>Sobreviva. Evolua. Derrote todos.</p>
    <button id="startButton">▶ JOGAR</button>
</div>

<canvas id="game"></canvas>

<script src="game.js"></script>

</body>
</html>
